import { PriorizarPipe } from './prriorizar.pipe';

describe('PrriorizarPipe', () => {
  it('create an instance', () => {
    const pipe = new PriorizarPipe();
    expect(pipe).toBeTruthy();
  });
});
